from setuptools import setup, find_packages

setup(
    name="achievement_bot",  # Name
    version="1.18.0",
    packages=find_packages()  # Automatically find the packages that are recognized in the '__init__.py'.
)